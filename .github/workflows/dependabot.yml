name: Dependabot schedule

on:
  schedule:
    - cron: '0 0 * * 0'

jobs:
  weekly-check:
    runs-on: ubuntu-latest
    steps:
      - name: Run weekly check
        run: echo "Dependabot workflow triggered"